apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: forge
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
# secret.yaml is applied separately — see secret.example.yaml
